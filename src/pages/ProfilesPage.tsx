import { profiles } from "../data/content";import { Page,PageHeader,Reveal } from "../components/Page";import { ArrowUpRight } from "../components/Icons";import { SocialIcon } from "../components/SocialIcon";
export function ProfilesPage(){return <Page mode="up"><div className="shell page-shell"><PageHeader eyebrow="Coding Profiles / 06" title="Practice, proof, and open work." text="Data Structures & Algorithms, Java, problem solving, LeetCode practice, and source code across my public developer profiles."/><div className="profiles-grid">{profiles.map((p,i)=><Reveal key={p.platform} delay={i*.08}><a href={p.href} target="_blank" rel="noopener noreferrer" className={`profile-card profile-card--${p.icon}`} aria-label={`View ${p.platform} profile`}><div className="profile-icon"><SocialIcon icon={p.icon}/></div><span>0{i+1}</span><h2>{p.platform}</h2><strong>{p.username}</strong><p>{p.blurb}</p><div className="text-action">View Profile <ArrowUpRight size={16}/></div></a></Reveal>)}</div></div></Page>}
