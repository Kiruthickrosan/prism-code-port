import { profiles } from "../data/content";import { Page,PageHeader,Reveal } from "../components/Page";import { ArrowUpRight,Icon } from "../components/Icons";
export function ProfilesPage(){return <Page mode="up"><div className="shell page-shell"><PageHeader eyebrow="Profiles / 05" title="Practice, proof, and open work." text="Places where problem solving, engineering projects, and technical growth are documented. Verified statistics can be added later without changing the layout."/><div className="profiles-grid">{profiles.map((p,i)=><Reveal key={p.platform} delay={i*.08}><a href={p.href} target="_blank" rel="noreferrer" className="profile-card"><div className="profile-icon"><Icon name={p.icon} size={26}/></div><span>0{i+1}</span><h2>{p.platform}</h2><strong>{p.username}</strong><p>{p.blurb}</p><small>Statistics not connected</small><div className="text-action">View Profile <ArrowUpRight size={16}/></div></a></Reveal>)}</div></div></Page>}
