import { articles } from "../data/content";import { Page,PageHeader,Reveal } from "../components/Page";import { ArrowUpRight } from "../components/Icons";
export function ArticlesPage(){return <Page mode="blur"><div className="shell page-shell"><PageHeader eyebrow="Articles / 05" title="Notes from the workbench." text="Technical writing about algorithms, Java, system-building, and the lessons that become clearer when they are explained."/><div className="articles-grid">{articles.map((a,i)=><Reveal key={a.title} delay={i*.06}><a className="article-card" href={a.href} target="_blank" rel="noreferrer"><div className="article-card__index">{String(i+1).padStart(2,"0")}</div><div><p className="eyebrow">{a.topic}</p><h2>{a.title}</h2><p>{a.description}</p><div className="tags">{a.tags.map(t=><span key={t}>{t}</span>)}</div></div><div className="article-meta">{a.date&&<span>{a.date}</span>}{a.time&&<span>{a.time}</span>}<span className="article-read">Read Article <ArrowUpRight size={15}/></span></div></a></Reveal>)}</div></div></Page>}
