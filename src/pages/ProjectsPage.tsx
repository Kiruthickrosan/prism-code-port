import { useState } from "react";import { AnimatePresence,motion } from "framer-motion";import { projects,type Project } from "../data/content";import { Page,PageHeader,Reveal } from "../components/Page";import { ProjectCard } from "../components/ProjectCard";import { ArrowUpRight,X } from "../components/Icons";
export function ProjectsPage(){const [selected,setSelected]=useState<Project|null>(null);const hasGithub=selected?.github.startsWith("https://")??false;const hasDemo=selected?.demo.startsWith("https://")??false;return <Page mode="depth"><div className="shell page-shell"><PageHeader eyebrow="Projects / 03" title="Built to solve, not decorate." text="Featured work across personal finance, retrieval systems, AI planning, and an animated developer portfolio."/><div className="project-grid">{projects.map((p,i)=><Reveal key={p.slug} delay={i*.06} className={p.featured?"project-span":""}><ProjectCard project={p} onOpen={setSelected}/></Reveal>)}</div></div><AnimatePresence>{selected&&<motion.div className="modal-backdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onMouseDown={()=>setSelected(null)}><motion.article className="project-modal" role="dialog" aria-modal="true" aria-label={`${selected.title} project details`} initial={{opacity:0,y:30,scale:.97}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:20,scale:.98}} onMouseDown={e=>e.stopPropagation()}><button className="modal-close" onClick={()=>setSelected(null)} aria-label="Close project details"><X/></button><p className="eyebrow">{selected.eyebrow}</p><h2>{selected.title}</h2><p className="modal-lead">{selected.description}</p><div className="architecture"><span>Architecture</span><p>{selected.architecture}</p><div className="architecture__flow"><b>CLIENT</b><i/><b>API</b><i/><b>SERVICES</b><i/><b>DATA</b></div></div><div className="modal-grid"><div><span>Problem</span><p>{selected.problem}</p></div><div><span>Solution</span><p>{selected.solution}</p></div><div><span>Challenges</span><p>{selected.challenges}</p></div><div><span>Results</span><p>{selected.results}</p></div></div>{(hasGithub||hasDemo)&&<div className="modal-actions">{hasGithub&&<a className="button button--primary" href={selected.github} target="_blank" rel="noopener noreferrer">GitHub <ArrowUpRight size={16}/></a>}{hasDemo&&<a className="button button--secondary" href={selected.demo} target="_blank" rel="noopener noreferrer">Live Demo <ArrowUpRight size={16}/></a>}</div>}</motion.article></motion.div>}</AnimatePresence></Page>}
