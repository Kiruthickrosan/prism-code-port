import { Link, useRouterState } from "@tanstack/react-router";
import { AnimatePresence, motion } from "framer-motion";
import { useEffect, useState } from "react";
import { site } from "../data/content"; import { Download, Menu, X } from "./Icons";
const links=[{to:"/",label:"Home"},{to:"/about",label:"About"},{to:"/experience",label:"Experience"},{to:"/projects",label:"Projects"},{to:"/skills",label:"Skills"},{to:"/articles",label:"Articles"},{to:"/coding-profiles",label:"Coding Profiles"},{to:"/contact",label:"Contact"}] as const;
export function Navbar(){const [open,setOpen]=useState(false),[scrolled,setScrolled]=useState(false);const path=useRouterState({select:s=>s.location.pathname});useEffect(()=>{const fn=()=>setScrolled(window.scrollY>24);fn();window.addEventListener("scroll",fn,{passive:true});return()=>window.removeEventListener("scroll",fn)},[]);useEffect(()=>setOpen(false),[path]);return <><header className={`navbar ${scrolled?"navbar--scrolled":""}`}><Link to="/" className="brand" aria-label="Home"><span>{site.shortName}</span><strong>{site.name}</strong></Link><nav className="navlinks" aria-label="Main navigation">{links.map(l=><Link key={l.to} to={l.to} className={path===l.to?"active":""}>{l.label}</Link>)}</nav><a className="resume-link" href={site.resume} download><Download size={16}/> Resume</a><button className="menu-button" onClick={()=>setOpen(!open)} aria-expanded={open} aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button></header><AnimatePresence>{open&&<motion.nav className="mobile-menu" initial={{opacity:0,y:-12}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-12}}>{links.map(l=><Link key={l.to} to={l.to}>{l.label}</Link>)}<a href={site.resume} download>Download Resume</a></motion.nav>}</AnimatePresence></>}
